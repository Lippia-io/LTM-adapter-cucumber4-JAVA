{
  "method": "POST",
  "url": "{{url}}",
  "endpoint": "tests",
  "headers": {
    "Content-Type": "application/json",
    "Authorization": "Bearer {{token}}"
  },
  "body": {
    "title": "{{title}}",
    "runId": "{{runId}}",
    "status": "{{status}}",
    "feature": "{{feature}}",
    "type": "{{type}}",
    "tags": "{{tags}}",
    "steps": "{{steps}}"
  }
}